<style>
    :root {
        --color-bg: #111111;;
        --color-bg-alt: #223333;
        --color-text: #ddddff;
        --color-text-alt: #8d99a8;

        --color-navbar-bg: #002222;
        --color-navbar-text: #eeeeee;

        --color-accent-1: #77ff77;
        --color-accent-2: #77dd77;
        --color-accent-3: #77dd44;

        --plastic-number-const: 1.32471;
        --cell-width: 8.5rem;
    }

    body {
        margin: 0;
        line-height: 1.0;
        min-height: 100vh;
        font-weight: 400;
        font-family: "Calibri", sans-serif;
        background-color: var(--color-bg);
        color: var(--color-text);
    }

    .container {
        max-width: 93%;
        margin-right: auto;
        margin-left: auto;
    }

    footer,
    navbar {
        position: relative;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        padding: 0.75rem 1.5rem;
        background-color: var(--color-navbar-bg);
        color: var(--color-navbar-text);
    }

    .shadow {
        transition: 0.3s;
        box-shadow: 0 0.25rem 0.75rem var(--color-accent-2) !important;
    }

    .card {
        padding: 6rem 3rem;
        text-align: center;
        background-color: var(--color-bg-alt);
    }

    .card>p {
        font-size: 1.2rem;
        margin-top: 1rem;
        margin-bottom: 1rem;
        width: 50%;
        margin-left: auto;
        margin-right: auto;
    }

    .grid-container {
        display: none;
    }

    .visible {
        display: block;
    }

    .lib {
        width: 100%;
        margin-top: 2rem;
        list-style: none;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;
    }

    .lib-cell {
        width: var(--cell-width);
        height: calc(var(--cell-width) * var(--plastic-number-const));
        margin: 1rem;
        border-radius: 0.5rem;
        background-color: var(--color-bg-alt);
        text-decoration: none;
    }

    .lib-img {
        width: 100%;
        aspect-ratio: 1/1;
        border-radius: 0.5rem 0.5rem 0 0;
        filter: brightness(0.85);
    }

    .lib-text {
        overflow: hidden;
        padding: 0.05rem 0.5rem;
        text-align: left;
        text-overflow: ellipsis;
        white-space: nowrap;
        text-align: center;
    }

    .lib-tex-first {
        padding-top: 0.3rem;
        font-size: 0.9rem;
    }

    .lib-text-second {
        padding-top: 0.15rem;
        font-size: 0.8rem;
        color: var(--color-text-alt);
    }

    h1,
    .h1 {
        font-size: 2.5rem;
        margin-top: 0;
        line-height: 1.1em;
    }

    h2,
    .h2 {
        font-size: 2rem;
        margin-top: 0;
        line-height: 1.2em;
    }

    h3,
    .h3 {
        font-size: 1.65rem;
        margin-top: 0;
        line-height: 1.3em;
    }

    h4,
    .h4 {
        font-size: 1.4rem;
        margin-top: 0;
        line-height: 1.4em;
    }

    h5,
    .h5 {
        font-size: 1.1rem;
        margin-top: 0;
        line-height: 1.5em;
    }

    h6,
    .h6 {
        font-size: 0.9rem;
        margin-top: 0;
        line-height: 1.6em;
    }

    a{
        color: var(--color-accent-1);
        text-decoration: none;
    }

    a:hover,
    a:focus {
        color: var(--color-accent-2);
        text-decoration: underline;
    }

    .btn {
        font-size: 1.1rem;
        background-color: transparent;
        border: 0.2rem solid var(--color-accent-3);
        border-radius: 1rem;
        padding: 0.6rem 1.1rem;
        margin: 0.6rem;
        color: var(--color-text-alt);
    }

    .btn:hover,
    .btn:focus,
    .active {
        background-color: var(--color-bg-alt);
        border-color: var(--color-accent-1);
        color: var(--color-text);
    }

    
</style>